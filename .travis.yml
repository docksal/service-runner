language: generic

services:
  - docker

env:
  global:
    - REPO=docksal/runner
    - VERSION=curl-git

install:
  - curl -fsSL get.docksal.io | sh
  - fin version
  - fin sysinfo

script:
  - export HOST_UID=$(id -u); export HOST_GID=$(id -g); make; make test

after_failure:
  - make logs
